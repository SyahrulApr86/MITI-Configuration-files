#!/bin/bash

# Berikut cara untuk masuk ke dalam terminal dari docker container
# sudo docker container exec -i -t [nama container atau id container] bash

# example:
sudo docker container exec -i -t db-db-1 bash
